# Evasion Browser 1.4.1

## GitHub Actions fix

- Replaced private build-environment npm URLs in `package-lock.json`
  with `https://registry.npmjs.org/`.
- Added npm network retries to Windows, Linux, and online-installer jobs.
- Updated project version to 1.4.1.

Use the release tag `v1.4.1`.
