# Reset Browser Data

Open **Profile Settings → Reset Evasion Browser**.

The reset requires the current profile password and an explicit confirmation. It removes the local profile, encrypted password vault, browser settings, bookmarks, history, saved sessions, notes, tasks, workspaces, cookies, cache and site storage, then restarts into first-time setup.

Data is removed only from the current computer. Remote backups or GitHub release files are not affected.
