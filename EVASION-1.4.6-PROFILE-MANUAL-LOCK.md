# Evasion Browser 1.4.6

- Removed the Profile **Auto-lock** control.
- Removed profile idle-time locking.
- The browser profile stays unlocked until the user clicks **Lock now**.
- Manual locking remains available.
- Password-manager local reset and manual vault lock remain included.
